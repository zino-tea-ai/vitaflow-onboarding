# Living Canvas - 产品规格文档

> 版本: 1.0.0  
> 更新日期: 2026-01-09  
> 作者: NogicOS Team

---

## 1. 产品概述

### 1.1 什么是 Living Canvas

Living Canvas 是 NogicOS 的核心交互界面，将 **Hook Map（神经网络可视化）** 与 **对话界面** 融为一体。它不再是传统的"侧边栏功能"，而是让用户在每次对话中都能直观感受到 AI 正在连接哪些应用、当前在做什么。

### 1.2 设计理念

**"Hook Map 不是功能，是体验的一部分。"**

顶级产品不会把核心差异化藏在侧边栏里：
- **Linear** 的动效融入主体验
- **Arc Browser** 的空间感无处不在
- **Figma** 的协作光标始终可见

Living Canvas 遵循同样的理念：AI 的连接状态是用户体验的一部分，不是"点击查看"的功能。

### 1.3 核心价值

| 价值点 | 说明 |
|--------|------|
| **可见性** | 用户始终能看到 AI 连接了哪些应用 |
| **直觉性** | 状态变化通过颜色和动画自然传达 |
| **沉浸感** | 神经网络背景创造"活"的感觉 |
| **专注性** | 对话仍是核心，背景不抢眼 |

---

## 2. 技术架构

### 2.1 三层结构

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           神经网络层 (Neural Background)             │   │
│  │                    固定高度 240px                    │   │
│  │     [App] ─────────── [AI] ─────────── [App]        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              对话层 (Content Layer)                  │   │
│  │                    flex-1 滚动                       │   │
│  │     ╭────────────────────────────────────────╮      │   │
│  │     │ User: 帮我填写这个表单                  │      │   │
│  │     ╰────────────────────────────────────────╯      │   │
│  │     ╭────────────────────────────────────────╮      │   │
│  │     │ AI: 好的，我来帮你...                   │      │   │
│  │     ╰────────────────────────────────────────╯      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              控制层 (Control Layer)                  │   │
│  │         状态条 + 输入框 + 发送按钮                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 组件结构

```
LivingCanvasChat
├── NeuralBackground (神经网络背景)
│   ├── SVG 连接线
│   ├── 应用节点 (AppNode)
│   └── 中心 AI 节点 (CenterNode)
├── Content Layer (对话层)
│   ├── Welcome Screen (空状态)
│   └── MessageBubble[] (消息气泡)
└── Control Layer (控制层)
    ├── StatusBar (状态条)
    └── InputArea (输入框)
```

### 2.3 文件位置

```
nogicos/nogicos-ui/src/components/nogicos/
├── LivingCanvasChat.tsx    # 主组件（集成 useChat）
├── LivingCanvas.tsx        # 纯 UI 组件（无状态管理）
├── HookMap.tsx             # 独立 Hook Map 窗口
└── HookMapPanel.tsx        # 侧边栏 Hook Map 面板
```

---

## 3. 状态管理

### 3.1 AI 状态 (AIState)

| 状态 | 颜色 | 图标 | 触发条件 |
|------|------|------|----------|
| `idle` | 翠绿 #10b981 | ⚡ Zap | 默认状态，等待输入 |
| `reading` | 天蓝 #0ea5e9 | 👁 Eye | 读取应用数据 |
| `thinking` | 紫色 #8b5cf6 | 🧠 Brain | 处理/生成回复 |
| `acting` | 琥珀 #f59e0b | ✏️ Pencil | 执行工具/操作 |
| `success` | 翠绿 #10b981 | ✨ Sparkles | 任务完成 |

### 3.2 状态映射逻辑

```typescript
const aiState: AIState = useMemo(() => {
  if (status === 'submitted') return 'thinking';
  if (status === 'streaming') {
    const lastMsg = chatMessages[chatMessages.length - 1];
    if (lastMsg?.parts?.some(p => p.type?.startsWith('tool-'))) {
      return 'acting';
    }
    return 'thinking';
  }
  return 'idle';
}, [status, chatMessages]);
```

### 3.3 数据流状态 (DataFlowState)

```typescript
interface DataFlowState {
  sourceHwnd: number | null;  // 源应用窗口句柄
  targetHwnd: number | null;  // 目标（null = NogicOS）
  direction: 'in' | 'out';    // 数据流向
  action?: string;            // 当前动作描述
}
```

---

## 4. 视觉规范

### 4.1 颜色系统

```typescript
const COLORS = {
  emerald: {
    primary: '#10b981',      // 主色
    light: '#34d399',        // 高亮
    dark: '#059669',         // 深色
    glow: 'rgba(16, 185, 129, 0.3)',  // 光晕
  },
  sky: {
    primary: '#0ea5e9',
    light: '#38bdf8',
    glow: 'rgba(14, 165, 233, 0.3)',
  },
  violet: {
    primary: '#8b5cf6',
    light: '#a78bfa',
    glow: 'rgba(139, 92, 246, 0.3)',
  },
  amber: {
    primary: '#f59e0b',
    light: '#fbbf24',
    glow: 'rgba(245, 158, 11, 0.3)',
  },
};
```

### 4.2 动画配置

```typescript
const SPRING = {
  gentle: { type: 'spring', stiffness: 120, damping: 14 },
  snappy: { type: 'spring', stiffness: 400, damping: 25 },
  smooth: { type: 'spring', stiffness: 200, damping: 20 },
  micro: { type: 'spring', stiffness: 500, damping: 35 },
};
```

### 4.3 关键动画效果

| 效果 | 触发条件 | 实现方式 |
|------|----------|----------|
| **呼吸脉动** | AI 空闲时 | 中心节点 scale 1.0→1.1→1.0，4s 循环 |
| **思考波纹** | AI 思考时 | 3 个同心圆向外扩散，2s 循环 |
| **数据脉冲** | 数据流动时 | 3 个光点沿曲线移动，1.2s 循环 |
| **图标切换** | 状态改变时 | scale 0→1 + rotate -180→0 |
| **扫描线** | 始终 | 中心节点内旋转，8s 循环 |

---

## 5. 连接线设计

### 5.1 贝塞尔曲线计算

```typescript
// 控制点偏移，让曲线有优雅的弧度
const midX = (x + centerX) / 2;
const midY = (y + centerY) / 2;
const dx = centerX - x;
const dy = centerY - y;
const dist = Math.sqrt(dx * dx + dy * dy);
const offset = dist * 0.12;  // 偏移量为距离的 12%
const perpX = -dy / dist * offset;
const perpY = dx / dist * offset;
const ctrlX = midX + perpX;
const ctrlY = midY + perpY;

// SVG 路径
const pathD = `M ${x} ${y} Q ${ctrlX} ${ctrlY} ${centerX} ${centerY}`;
```

### 5.2 连接线样式

| 状态 | 颜色 | 线宽 |
|------|------|------|
| 默认 | `rgba(16, 185, 129, 0.3)` | 1px |
| 活跃（数据流入） | `#38bdf8` (sky) | 2px + 发光 |
| 活跃（数据流出） | `#fbbf24` (amber) | 2px + 发光 |

---

## 6. 消息气泡设计

### 6.1 用户消息

```css
.user-bubble {
  background: rgba(16, 185, 129, 0.15);  /* 翠绿色半透明 */
  border: 1px solid rgba(16, 185, 129, 0.2);
  backdrop-filter: blur(12px);
  border-radius: 16px;
  margin-left: auto;  /* 右对齐 */
}
```

### 6.2 AI 消息

```css
.ai-bubble {
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(12px);
  border-radius: 16px;
  margin-right: auto;  /* 左对齐 */
}
```

### 6.3 工具执行显示

```
╭─────────────────────────────────────╮
│ NogicOS                             │
│ ✓ playwright snapshot               │
│ ✓ desktop screenshot                │
│─────────────────────────────────────│
│ 好的，我已经获取了页面截图...        │
╰─────────────────────────────────────╯
```

---

## 7. 空状态设计

当没有连接应用时：

```
        ╭───────╮
        │  ⚡   │  ← 中心节点（呼吸动画）
        ╰───────╯
         NogicOS
          Ready
   Connect apps to see the magic
```

- 中心节点保持呼吸动画
- 显示 "Ready" 状态
- 下方提示 "Connect apps to see the magic"

---

## 8. Props 接口

### 8.1 LivingCanvasChat Props

```typescript
interface LivingCanvasChatProps {
  // API 配置
  apiUrl?: string;           // 默认 'http://localhost:8080/api/chat'
  sessionId: string;         // 会话 ID
  
  // Hook 相关
  connectedApps: ConnectedApp[];  // 已连接的应用列表
  dataFlow?: DataFlowState;       // 当前数据流状态
  
  // 消息
  initialMessages?: ChatMessage[];  // 初始消息（历史记录）
  
  // 回调
  onSessionUpdate?: (title: string, preview: string) => void;
  onMessagesChange?: (messages: ChatMessage[]) => void;
  
  // 样式
  className?: string;
}
```

### 8.2 ConnectedApp 类型

```typescript
interface ConnectedApp {
  hwnd: number;              // Windows 窗口句柄
  title: string;             // 窗口标题
  app_name: string;          // 应用进程名
  app_display_name: string;  // 显示名称（如 "Chrome"）
  is_browser: boolean;       // 是否是浏览器
  connected_at: string;      // 连接时间 ISO 字符串
}
```

---

## 9. 集成指南

### 9.1 在 App.tsx 中使用

```tsx
import { LivingCanvasChat } from '@/components/nogicos';

// 在 App.tsx 中
{CHAT_MODE === 'living' && (
  <div className="flex-1 flex min-h-0">
    <Sidebar
      sessions={sessions}
      onConnectedAppsChange={setConnectedApps}
      // ... other props
    />
    <LivingCanvasChat
      apiUrl="http://localhost:8080/api/chat"
      sessionId={activeSessionId}
      connectedApps={connectedApps}
      initialMessages={chatSessions[activeSessionId] || []}
      onSessionUpdate={handleSessionUpdate}
      onMessagesChange={handleMessagesChange}
      className="flex-1"
    />
  </div>
)}
```

### 9.2 切换模式

在 `App.tsx` 中修改 `CHAT_MODE`:

```typescript
type ChatMode = 'living' | 'minimal' | 'system' | 'premium' | 'cursor' | 'chatkit' | 'legacy';
const CHAT_MODE: ChatMode = 'living';  // ← 切换到 Living Canvas
```

---

## 10. 已知限制

| 限制 | 说明 | 计划修复 |
|------|------|----------|
| 最多显示 8 个应用节点 | 超过时布局会拥挤 | 考虑添加滚动/折叠 |
| 连接线动画性能 | SMIL 动画在某些浏览器有问题 | 考虑改用 CSS/JS 动画 |
| 神经网络高度固定 | 240px 可能在小屏幕上过大 | 添加响应式适配 |

---

## 11. 未来规划

### Phase 2
- [ ] 数据流动画（实时显示读取/写入哪个应用）
- [ ] 任务完成时的 Sparkles 粒子效果
- [ ] 应用节点 hover 显示详细信息

### Phase 3
- [ ] 多中心节点（支持多 Agent）
- [ ] 历史轨迹回放
- [ ] 自定义颜色主题

---

## 12. 联系方式

如有问题，请联系：
- **产品**: Zino
- **前端**: NogicOS Team
- **后端**: NogicOS Team

---

*本文档由 NogicOS AI 助手协助生成*
