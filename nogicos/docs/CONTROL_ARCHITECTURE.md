# NogicOS 控制权管理架构

> 基于 ByteBot、Microsoft UFO、Anthropic Computer Use 等项目的最佳实践

## 概述

NogicOS 采用**原生 Windows 环境**运行，而非虚拟化方案。这带来了更好的性能和用户体验，但也需要更精细的控制权管理机制。

### 设计目标

1. **用户永远优先** - AI 操作绝不能干扰用户的正常使用
2. **并行不冲突** - 用户和 AI 可以同时操作不同窗口
3. **风险可控** - 敏感操作需要确认，危险操作被禁止
4. **全程可见** - 通过 Overlay 显示 AI 的意图和状态
5. **一键停止** - 任何时候都能立即中断 AI

---

## 参考项目分析

### 1. ByteBot (bytebot-ai/bytebot)

**架构特点**：
- 虚拟 Ubuntu 桌面（Docker 容器）
- Agent 循环：Observe → Act → Verify
- 统一的 `/computer-use` API 端点
- `@nut-tree-fork/nut-js` 底层自动化

**我们借鉴**：
- Agent 循环模式
- 工具定义规范
- 上下文压缩机制

**我们不采用**：
- 虚拟环境（我们用原生 Windows）

### 2. Microsoft UFO (microsoft/UFO)

**架构特点**：
- FSM 状态机管理控制流
- PENDING 状态：等待用户输入
- CONFIRM 状态：敏感操作确认
- Picture-in-Picture 桌面隔离
- 分层 Agent：HostAgent + AppAgent

**我们借鉴**：
- FSM 状态机设计
- PENDING/CONFIRM 状态
- 分层 Agent 架构
- 敏感操作确认机制

### 3. Anthropic Computer Use Demo

**架构特点**：
- Docker 容器化隔离
- 最小权限原则
- 工具级别验证
- 人工确认关键操作
- 提示注入警告

**我们借鉴**：
- 安全警告机制
- 工具验证逻辑
- 人工确认流程

### 4. PyAutoGUI

**架构特点**：
- `FAILSAFE = True`：鼠标移到左上角触发中断
- `PAUSE`：每次操作后全局暂停

**我们借鉴**：
- Fail-Safe 紧急停止机制

---

## 核心架构：金字塔分层控制

```
                    ┌─────────┐
                    │ 紧急层  │  L0 - 绝对优先，立即生效
                    │         │
                ┌───┴─────────┴───┐
                │    物理层 L1    │  检测用户输入，自动暂停
            ┌───┴─────────────────┴───┐
            │      窗口层 L2          │  隔离 AI 操作范围
        ┌───┴─────────────────────────┴───┐
        │         操作层 L3               │  风险分级确认
    ┌───┴─────────────────────────────────┴───┐
    │             反馈层 L4                   │  Overlay 可视化
    └─────────────────────────────────────────┘
```

### L0: 紧急层 (Emergency Layer)

**来源**：PyAutoGUI Fail-Safe

**机制**：
- 全局快捷键 `Ctrl+Shift+P` - 立即暂停所有 AI 操作
- 连按 3 次 `Escape` - 备用触发
- 鼠标移到左上角停留 500ms - 物理触发
- 响应时间 < 100ms

```typescript
const EMERGENCY_CONFIG = {
  keyboard: {
    primary: 'Ctrl+Shift+P',
    backup: 'Escape x 3',
  },
  mouseCorner: {
    position: 'top-left',
    duration: 500,
  },
  responseTime: 100,
};
```

### L1: 物理层 (Physical Input Layer)

**来源**：User-First 模式 + UFO PENDING 状态

**机制**：
- 通过 Windows 低级钩子监听用户输入
- 使用 `dwExtraInfo` 标记区分 AI 输入和用户输入
- 检测到用户输入 → 立即暂停 AI
- 3 秒无用户输入 → 自动恢复

```typescript
// AI 输入标记
const NOGICOS_INPUT_MARKER = 0x4E4F4749; // "NOGI"

// 判断是否是用户输入
function isUserInput(extraInfo: number): boolean {
  return extraInfo !== NOGICOS_INPUT_MARKER;
}
```

**状态机**（参考 UFO）：

```
┌──────────────┐    检测到用户输入    ┌──────────────┐
│  AI_ACTIVE   │ ─────────────────→  │  AI_PENDING  │
│              │ ←─────────────────  │              │
└──────────────┘    3秒无输入/恢复    └──────────────┘
      │                                    │
      │ 敏感操作                           │ 用户明确接管
      ▼                                    ▼
┌──────────────┐                    ┌──────────────┐
│  AI_CONFIRM  │                    │ USER_CONTROL │
│  等待确认    │                    │  完全接管    │
└──────────────┘                    └──────────────┘
```

### L2: 窗口层 (Window Isolation Layer)

**来源**：窗口隔离模式 + UFO PiP 概念

**核心技术**：使用 Windows 消息 API 而非物理输入

```typescript
// ❌ 物理输入（会移动鼠标）
SendInput(...)  // 全局影响

// ✅ 窗口消息（只影响目标窗口）
PostMessage(hwnd, WM_LBUTTONDOWN, ...)  // 不移动物理鼠标
```

**机制**：
- AI 只能操作已连接的窗口
- 通过 `PostMessage`/`SendMessage` 发送操作
- 物理鼠标/键盘完全不受影响
- 用户可以同时操作其他窗口

**限制**：
- 部分操作（拖拽、某些特殊控件）需要物理输入
- 需要物理输入时，请求用户确认

### L3: 操作层 (Action Classification Layer)

**来源**：UFO CONFIRM 状态 + Anthropic 安全机制

**风险分级**：

| 风险等级 | 操作示例 | 处理方式 |
|---------|---------|---------|
| SAFE | 滚动、复制、查看 | 自动执行 |
| MODERATE | 保存、发送消息、编辑 | 执行但通知 |
| HIGH | 删除、发邮件、发布 | 需要确认 |
| FORBIDDEN | 输入密码、金融操作 | 禁止执行 |

```typescript
enum ActionRisk {
  SAFE,       // 自动执行
  MODERATE,   // 执行但通知
  HIGH,       // 需要确认
  FORBIDDEN   // 禁止执行
}
```

### L4: 反馈层 (Visual Feedback Layer)

**来源**：Overlay 交互模式

**机制**：
- 通过 Overlay 窗口显示 AI 状态
- 显示 AI 的意图（即将执行什么）
- 显示执行进度
- 显示完成/错误状态

---

## 安全网系统 (Safety Net)

### 1. 预操作快照

```typescript
interface OperationSnapshot {
  timestamp: Date;
  targetWindow: number;
  action: string;
  beforeState: {
    screenshot?: Buffer;
    clipboard?: string;
    fileHashes?: Map<string, string>;
  };
}
```

### 2. 敏感操作确认

```typescript
// 高风险操作需要用户确认
async function executeHighRiskAction(action: Action): Promise<Result> {
  const confirmed = await showConfirmationOverlay({
    title: '确认操作',
    message: `AI 想要：${action.description}`,
    details: action.details,
    timeout: 10000,  // 10秒超时自动取消
  });
  
  if (!confirmed) {
    return Result.cancelled();
  }
  
  return await execute(action);
}
```

### 3. 操作追踪

```typescript
interface OperationLog {
  id: string;
  timestamp: Date;
  action: string;
  target: string;
  result: 'success' | 'failed' | 'cancelled';
  snapshot?: OperationSnapshot;
  canUndo: boolean;
}
```

### 4. 回滚能力

- 文件操作：移动到回收站而非直接删除
- 文本操作：保留撤销历史
- 设置修改：保存原始值

---

## 信任等级系统

| 等级 | 描述 | 自动执行范围 |
|------|------|------------|
| Observer | 只读模式 | 无 |
| Assistant | 辅助模式 | SAFE 操作 |
| Operator | 操作模式 | SAFE + MODERATE |
| Trusted | 信任模式 | SAFE + MODERATE + HIGH (仍需确认) |

---

## 窗口消息 vs 物理输入

### 窗口消息（优先使用）

```typescript
// 点击窗口内的元素
function clickInWindow(hwnd: number, x: number, y: number) {
  const lParam = (y << 16) | (x & 0xFFFF);
  PostMessage(hwnd, WM_LBUTTONDOWN, MK_LBUTTON, lParam);
  PostMessage(hwnd, WM_LBUTTONUP, 0, lParam);
}

// 在窗口内输入文字
function typeInWindow(hwnd: number, text: string) {
  for (const char of text) {
    PostMessage(hwnd, WM_CHAR, char.charCodeAt(0), 0);
  }
}
```

**优点**：
- 不移动物理鼠标/键盘
- 用户可以同时操作其他窗口
- 窗口不需要在前台

**限制**：
- 部分应用不响应窗口消息
- 拖拽操作需要物理输入
- 某些安全软件会阻止

### 物理输入（需要时使用）

```typescript
// 当窗口消息失败时，请求物理输入权限
async function fallbackToPhysicalInput(action: Action): Promise<Result> {
  const allowed = await requestPhysicalInputPermission({
    reason: '此操作需要使用物理鼠标',
    duration: '约 5 秒',
  });
  
  if (!allowed) {
    return Result.cancelled();
  }
  
  // 暂时接管物理输入
  return await executeWithPhysicalInput(action);
}
```

---

## 并行工作流程

```
用户说："帮我整理 Chrome 书签，我去写代码"

┌─────────────────────────────────────────────────────────────┐
│                         时间线                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  0:00  AI: 连接 Chrome 窗口                                  │
│        用户: 切换到 VS Code                                  │
│                                                             │
│  0:01  AI: PostMessage → Chrome (点击书签)                  │
│        用户: 在 VS Code 打字 (不受影响)                      │
│                                                             │
│  0:02  AI: PostMessage → Chrome (整理书签)                  │
│        用户: 继续写代码                                      │
│                                                             │
│  0:10  AI: 完成，发送通知                                    │
│        用户: 收到通知，可选择查看                            │
│                                                             │
│  结果: 两边独立完成，互不干扰 ✅                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 场景处理策略

### 场景 1: 用户点击了 AI 正在操作的窗口

```
检测到用户点击 AI 工作窗口
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  ⏸ AI 已暂停                                                │
│                                                             │
│  您点击了 AI 正在操作的窗口                                  │
│  当前任务：整理书签 (45/100)                                │
│                                                             │
│  [继续 AI 任务]  [我来接管]  [取消任务]                      │
│                                                             │
│  💡 3 秒后自动恢复 AI 任务...                                │
└─────────────────────────────────────────────────────────────┘
```

### 场景 2: AI 需要操作用户正在用的窗口

```
AI 需要访问用户正在操作的窗口
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  🤖 AI 请求访问 VS Code                                      │
│                                                             │
│  任务：修改 config.ts 文件                                  │
│  预计时间：约 30 秒                                          │
│                                                             │
│  [允许，我先暂停]  [稍后再说]  [我来做]                      │
└─────────────────────────────────────────────────────────────┘
```

### 场景 3: 敏感操作确认

```
AI 请求执行高风险操作
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  ⚠️ AI 请求执行敏感操作                                      │
│                                                             │
│  操作：删除 23 个文件                                        │
│  位置：C:\Users\Zino\Downloads\                             │
│                                                             │
│  [✓ 允许删除]  [👁 预览]  [✗ 取消]  [🤚 我来操作]           │
│                                                             │
│  ⏱️ 10秒后自动取消                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 默认配置

```typescript
const DEFAULT_CONTROL_CONFIG = {
  // L0: 紧急层
  emergency: {
    enabled: true,
    keyboard: 'Ctrl+Shift+P',
    mouseCorner: { position: 'top-left', duration: 500 },
    responseTime: 100,
  },
  
  // L1: 物理层
  userFirst: {
    enabled: true,
    pauseOnUserInput: true,
    resumeDelay: 3000,
    inputMarker: 0x4E4F4749,
  },
  
  // L2: 窗口层
  windowIsolation: {
    enabled: true,
    onlyOperateConnected: true,
    preferWindowMessages: true,
    pauseOnWindowFocusLoss: true,
  },
  
  // L3: 操作层
  actionPolicy: {
    safe: 'auto_execute',
    moderate: 'execute_with_notice',
    high: 'require_confirmation',
    forbidden: 'manual_only',
    confirmTimeout: 10000,
  },
  
  // L4: 反馈层
  overlay: {
    enabled: true,
    showIntent: true,
    showProgress: true,
    showCompletion: true,
  },
  
  // 信任等级
  trustLevel: 'assistant',
};
```

---

## 实现路线图

### Phase 1: 基础控制 (1周)
- [ ] 紧急停止机制 (Ctrl+Shift+P)
- [ ] User-First 用户输入检测
- [ ] 基础状态机 (ACTIVE/PENDING)

### Phase 2: 窗口隔离 (1周)
- [ ] 窗口消息输入实现
- [ ] 多窗口独立管理
- [ ] 物理输入回退机制

### Phase 3: 操作分级 (1周)
- [ ] 风险分类系统
- [ ] 确认 Overlay UI
- [ ] 操作日志记录

### Phase 4: 完整反馈 (1周)
- [ ] 意图显示 Overlay
- [ ] 进度显示
- [ ] 完成/错误反馈

---

## 附录：Windows API 参考

### 窗口消息常量

```c
#define WM_LBUTTONDOWN  0x0201
#define WM_LBUTTONUP    0x0202
#define WM_RBUTTONDOWN  0x0204
#define WM_RBUTTONUP    0x0205
#define WM_MOUSEMOVE    0x0200
#define WM_MOUSEWHEEL   0x020A
#define WM_KEYDOWN      0x0100
#define WM_KEYUP        0x0101
#define WM_CHAR         0x0102
#define MK_LBUTTON      0x0001
```

### 关键函数

```c
// 发送消息（异步，不等待）
BOOL PostMessageW(HWND hWnd, UINT Msg, WPARAM wParam, LPARAM lParam);

// 发送消息（同步，等待返回）
LRESULT SendMessageW(HWND hWnd, UINT Msg, WPARAM wParam, LPARAM lParam);

// 模拟物理输入
UINT SendInput(UINT cInputs, LPINPUT pInputs, int cbSize);

// 设置低级钩子
HHOOK SetWindowsHookExW(int idHook, HOOKPROC lpfn, HINSTANCE hmod, DWORD dwThreadId);
```

---

## 参考资料

1. [ByteBot GitHub](https://github.com/bytebot-ai/bytebot)
2. [Microsoft UFO GitHub](https://github.com/microsoft/UFO)
3. [Anthropic Computer Use Demo](https://github.com/anthropics/anthropic-quickstarts/tree/main/computer-use-demo)
4. [PyAutoGUI Documentation](https://pyautogui.readthedocs.io/)
5. [Windows Message Reference](https://docs.microsoft.com/en-us/windows/win32/winmsg/about-messages-and-message-queues)
