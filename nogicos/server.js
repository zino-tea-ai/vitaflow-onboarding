"const express = require('express'); const app = express(); app.use(express.json({limit:'50mb'})); app.post('/api/chat', (req,res) => { const {message,conversationId} = req.body; console.log('Len:', message?.length||0); if(!message) return res.status(400).json({error:'required'}); res.json({success:true, len:message.length, id:conversationId}); }); app.listen(3000, ()=>console.log('Ready'));" 
